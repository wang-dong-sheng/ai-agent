<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dswang.aiagent.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.dswang.aiagent.domain.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, username, password_hash, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.dswang.aiagent.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, created_at, updated_at)
        VALUES (
            #{username},
            #{passwordHash},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <update id="update" parameterType="com.dswang.aiagent.domain.User">
        UPDATE users
        SET
            username = #{username},
            password_hash = #{passwordHash},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

</mapper>